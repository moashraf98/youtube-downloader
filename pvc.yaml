---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-downloader-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeName: youtube-downloader-pv
